apiVersion: v1
kind: Service
metadata:
  name: cloudjob-backend-service
spec:
  selector:
    app: cloudjob-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP